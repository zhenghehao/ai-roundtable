# AI圆桌产品说明

AI圆桌是一款本地优先的中文 AI 群聊讨论软件。它不是普通的单 AI 对话工具，而是让用户创建多个 AI 角色，让这些角色像一个小型团队一样围绕话题讨论、分工、写作、审核、总结，并在需要时生成可下载文件。

它适合不会写复杂提示词的小白用户，也适合需要把多个 AI 角色组织成工作流的创作者、产品经理、创业者、研究者和内容团队。

## 一句话介绍

AI圆桌可以让你把多个 AI 角色放进同一个群聊，让他们按顺序讨论问题、互相补充、分工协作，最后输出总结、文章、方案或可下载文件。

## 核心特点

- 本地优先：模型配置、角色、房间和聊天记录保存在本机浏览器或桌面应用本地。
- 多模型支持：可配置 OpenAI Compatible、Anthropic Claude 等协议的模型服务。
- 多角色协作：可以创建主持人、产品经理、技术专家、反对者、总结员、写手、审核员、文件大师等角色。
- 多群聊房间：不同房间保留独立参与角色、讨论轮数和聊天历史。
- @任务流：在输入框里直接 `@角色名`，让指定角色按顺序完成任务。
- 附件读取：聊天窗口支持上传 PDF、DOCX、XLSX、PPTX、TXT、MD、CSV、图片、HTML 等文件。
- 文件生成：通过“文件大师”等角色，把最终结果整理成 Word、Excel、Markdown、TXT、CSV、HTML 等可下载文件。
- 多语言界面：支持中文、英文、日语、西班牙语、法语、德语、葡萄牙语、俄语、阿拉伯语、韩语、意大利语、荷兰语等界面语言。

## 可以用来做什么

### 1. 选题和创意讨论

你可以让多个创意角色围绕一个方向讨论，比如：

- 短视频选题
- 小红书标题
- 公众号文章方向
- 产品宣传角度
- 品牌故事
- 营销活动方案

示例：

```text
@文案选题专家1号 @文案选题专家2号 你们先讨论三轮，围绕 AI 工具创业方向给我 5 个选题。
然后 @总结员 选出最适合落地的 1 个。
```

### 2. 写作工作流

可以把写作拆成多个角色：

- 选题专家负责确定方向
- 写手负责正文
- 审核员负责查问题
- 修正员负责修改
- 总编负责最终确认

示例：

```text
@选题专家 先给我确定一个文章选题。
然后 @高级写手 写一篇面向普通用户的文章。
然后 @审核员 检查逻辑、表达和事实风险。
然后 @修正员 根据审核意见修改。
最后 @文件大师 整理成 Word 文件，标题 24 号，正文 11 号。
```

### 3. 产品评审

适合用于评估一个产品想法是否值得做。

常见角色：

- 产品经理：看用户需求和体验
- 技术专家：看实现难度和技术风险
- 反对者：提出质疑和隐藏问题
- 创意顾问：提出差异化方案
- 总结员：收束结论和下一步

示例：

```text
我们要不要做一个面向小白用户的 AI 群聊软件？
请从用户需求、技术成本、商业价值和风险角度讨论两轮，最后给出是否值得继续做的判断。
```

### 4. 技术方案讨论

可以让不同技术角色围绕一个实现方案讨论：

- 架构设计
- 技术选型
- API 调用方案
- 本地存储方案
- 桌面端打包
- 安全和隐私风险

示例：

```text
@技术专家 @反对者 你们先讨论两轮：这个产品是否应该第一版只用 localStorage，不做数据库？
然后 @总结员 给出最终技术建议。
```

### 5. 报告和文档生成

通过“文件大师”角色，可以把前面讨论整理成正式文件。

支持的文件类型包括：

- Word：`.docx`
- Excel：`.xlsx`
- Markdown：`.md`
- 文本：`.txt`
- 表格：`.csv`
- 网页：`.html`

示例：

```text
@文件大师 请把前面讨论整理成一份 Word 报告。
要求：
标题 24 号，二级标题 16 号，正文 11 号。
结构包括：背景、核心结论、执行方案、风险点、下一步行动。
```

## 主要功能说明

### 模型配置

用户可以添加多个模型供应商配置，包括：

- 配置名称
- 协议类型
- Base URL
- API Key
- 默认模型
- 备注

支持 OpenAI Compatible 协议的供应商，也支持 Anthropic Claude 协议。

常见可配置供应商包括：

- OpenAI
- DeepSeek
- 通义千问 Qwen
- Kimi
- GLM 智谱
- 豆包火山方舟
- 百度千帆
- 腾讯混元
- MiniMax
- Gemini
- Grok
- Claude

API Key 只保存在本地，不会上传到 AI圆桌自己的服务器。

### 角色管理

用户可以创建和编辑 AI 角色，每个角色可以设置：

- 角色名称
- 头像颜色或本地头像
- 身份设定
- 发言风格
- 默认供应商配置
- 默认模型
- 是否启用
- Markdown 身份文件

上传 Markdown 身份文件后，模型每次发言都会优先读取该文件。身份文件会作为角色的最高优先级设定，如果补充设定和身份文件冲突，以身份文件为准。

### 群聊房间

用户可以创建多个群聊房间，每个房间都有独立的：

- 房间名称
- 参与角色
- 发言顺序
- 默认讨论轮数
- 聊天记录

新建群聊时，可以选择哪些角色加入，并拖动调整发言顺序。

### 私聊

除了群聊，也可以创建私聊房间。私聊适合和某一个固定身份进行深度交流，比如：

- 和“马斯克风格顾问”聊产品战略
- 和“投资人”聊商业模式
- 和“写作导师”修改文章
- 和“技术专家”讨论代码架构

### @任务流

AI圆桌支持在聊天输入框里直接用 `@角色名` 指定任务顺序。

例如：

```text
@产品经理 @创意顾问 你们先对话三轮，探讨一个适合普通用户的 AI 产品选题。
然后 @总结员 总结一个最终可落地的选题。
然后 @高级写手 写一篇文章。
然后 @审核员 进行审核。
然后 @修正员 根据审核意见修正。
最后 @文件大师 整理成 Word 文件。
```

系统会按顺序执行：

```text
产品经理、创意顾问讨论 3 轮
→ 总结员总结
→ 高级写手写作
→ 审核员审核
→ 修正员修正
→ 文件大师生成文件
```

### 附件能力

聊天窗口支持上传常见文件，AI 可以结合附件内容进行讨论。

支持格式包括：

- PDF
- DOCX
- XLSX
- PPTX
- TXT
- MD
- CSV
- PNG
- JPG
- JPEG
- WEBP
- HTML

对于可读取文本的文件，系统会尽量提取内容并传给模型。对于图片，如果模型支持多模态能力，也可以结合图片进行分析。

### 文件大师

“文件大师”是专门负责最终文件交付的角色。

它适合在流程最后使用，例如：

- 把讨论结果整理成 Word 报告
- 把表格内容整理成 Excel
- 把文章整理成 Markdown
- 把会议结论整理成 TXT
- 把网页内容整理成 HTML

文件大师不会默认加入普通群聊，需要用户主动把它加入房间，并在需要时通过 `@文件大师` 调用。

## 推荐使用流程

1. 添加模型配置  
   先配置至少一个模型供应商，例如 DeepSeek、OpenAI、Qwen、Kimi、Claude 等。

2. 创建角色  
   创建你需要的角色，例如选题专家、写手、审核员、总结员、文件大师。

3. 创建群聊  
   选择要加入的角色，并调整他们的发言顺序。

4. 输入任务  
   直接输入讨论主题，或者用 `@角色名` 指定任务流。

5. 生成总结或文件  
   讨论完成后，可以让总结员总结，也可以让文件大师整理成可下载文件。

## 示例工作流

### 内容创作

```text
@选题专家 给我 5 个适合小红书的 AI 工具选题。
然后 @总结员 选出最有传播潜力的 1 个。
然后 @高级写手 写成一篇小红书风格文章。
然后 @审核员 检查标题、逻辑和敏感风险。
最后 @文件大师 整理成 Word 文件。
```

### 产品评审

```text
@产品经理 @技术专家 @反对者 你们围绕“AI群聊协作工具”讨论两轮。
重点看用户需求、实现成本、商业价值和风险。
然后 @总结员 输出最终结论。
```

### 报告整理

```text
@文件大师 请把前面的讨论整理成一份 Markdown 报告。
结构包括：背景、核心观点、主要分歧、风险、下一步计划。
```

## 隐私和数据

AI圆桌第一版是本地单机使用：

- 不需要登录
- 不需要数据库
- 不做云同步
- 角色、房间、聊天记录保存在本地
- API Key 只保存在本地浏览器或桌面应用环境

请妥善保管自己的 API Key，不要把包含个人 API Key 的配置文件或截图公开发布。

## 注意事项

- 不同模型供应商的能力不同，有些模型不支持图片、多模态或长文本。
- 如果模型额度不足、限速或模型名无权限，可能会导致调用失败。
- 文件生成功能依赖模型按指定格式输出，文件大师角色会优先使用这种格式。
- 当前桌面版未做 Apple 官方签名和公证，macOS 首次打开时可能需要右键点击应用并选择“打开”。

## 适合人群

- 内容创作者
- 自媒体团队
- 产品经理
- 创业者
- 运营人员
- 研究者
- 写作者
- 独立开发者
- 想把多个 AI 组织成一个“虚拟团队”的用户

## 产品定位

AI圆桌不是单纯的聊天机器人，而是一个本地 AI 角色协作工作台。

它的目标是让用户用更自然的方式组织 AI：

- 像拉微信群一样创建多个角色
- 像开会一样让角色轮流讨论
- 像分配任务一样用 `@` 指派工作
- 像交付成果一样生成总结和文件

最终，AI圆桌希望帮助用户把一个模糊想法，变成经过多角色讨论、审核和整理后的可执行结果。
